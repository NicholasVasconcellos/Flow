Recovery. Previous stage left uncommitted changes. Stage and commit. Stop.

git status — confirm dirty.
git add -A.
git commit -m "<imperative ≤72 chars>" — subject grounded in diff. Several distinct changes → blank line + one bullet per change.
Stop.

Clean tree on arrival → exit silently.
Merge markers / half-applied refactor → FLOW_BLOCKED: <one-sentence reason>.

Don't: run tests. edit files. push. anything beyond the commit.
